(
    key_map: {
        FPS(MoveBackward): Some(S),
        FPS(Sprint): Some(LShift),
        Build(LevelUp): Some(R),
        FPS(Jump): Some(Space),
        FPS(MoveLeft): Some(A),
        FPS(MoveForward): Some(W),
        Build(LevelDown): Some(F),
        FPS(Interact): Some(E),
        FPS(Crouch): Some(LControl),
        FPS(MoveRight): Some(D),
    },
)