(
    key_map: {
        FPS(Crouch): Some(LControl),
        FPS(Interact): Some(E),
        FPS(MoveRight): Some(D),
        FPS(Jump): Some(Space),
        FPS(MoveBackward): Some(S),
        Build(LevelDown): Some(F),
        Build(MoveLeft): Some(A),
        Build(RotateCounterClockwise): Some(E),
        Build(MoveRight): Some(D),
        Build(LevelUp): Some(R),
        Build(RotateClockwise): Some(Q),
        Build(MoveBackward): Some(S),
        FPS(MoveForward): Some(W),
        FPS(Sprint): Some(LShift),
        FPS(MoveLeft): Some(A),
        Build(MoveForward): Some(W),
    },
)